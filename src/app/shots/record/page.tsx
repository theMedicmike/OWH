import AppShell from "@/components/AppShell";
import LocatorCard from "@/components/LocatorCard";

export default function ShotRecordLocatorPage() {
  return (
    <AppShell title="Where your record actually is">
      <LocatorCard />
    </AppShell>
  );
}
